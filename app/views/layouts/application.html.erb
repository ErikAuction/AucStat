<!DOCTYPE html>
<html>
  <head>
    <title>AucStat</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-inverse" id="aucStatNav">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="#">AucStat</a>
        </div>
 
     <ul class="nav navbar-nav" >
      <li class="active"><a href="#">Home</a></li>                              
      <li>    <%= link_to 'All Stats', auctions_path, remote: true %> </li>     <%# button that will render the 'allStats' partial for complete table listing of all auctions %>
     </ul>

     </div>
    </nav>

    <div class="container-fluid">
      <div class="row content">
        <div class="col-sm-1 sidenav">          <%#sidenav div purely used for styling or potential future use %>
        </div>

        <div class="col-sm-10 "> 
          <div id="importdiv">
            <h3>Import a CSV File</h3>
            <%= form_tag({controller: "importer", action: "import"},  method: "post", multipart: true) do %>
                <%= file_field_tag :file %>
                <%= submit_tag("Import CSV", :class=>'btn-info') %>
              <% end %>
          </div>
          <%= yield %>
        </div>

        <div class="col-sm-1 sidenav">          <%#sidenav div purely used for styling or potential future use %>
        </div>

      </div>
    </div>

<footer class="container-fluid text-center">
  <p>Erik Hutchinson 2017</p>
</footer>
  </body>
</html>
